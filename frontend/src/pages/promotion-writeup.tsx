import { SurfacePlaceholder } from '@/components/surface-placeholder'

export function PromotionWriteup() {
  return (
    <SurfacePlaceholder
      surface="Promotion Writeup"
      description="Pre-submission check on promotion justifications: bias in the language, plus drift against historical peer feedback for that employee."
    />
  )
}
