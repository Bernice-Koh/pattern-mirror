import { SurfacePlaceholder } from '@/components/surface-placeholder'

export function JdStudio() {
  return (
    <SurfacePlaceholder
      surface="JD Studio"
      description="Real-time bias flagging while writing a job description — dictionary flags in under 100ms, with an LLM contextual pass streaming in after a typing pause."
    />
  )
}
