import { SurfacePlaceholder } from '@/components/surface-placeholder'

export function FeedbackCheckpoint() {
  return (
    <SurfacePlaceholder
      surface="Feedback Checkpoint"
      description="Pre-submission check on interview feedback: bias in the language, plus drift against the criteria stated in the original job description."
    />
  )
}
