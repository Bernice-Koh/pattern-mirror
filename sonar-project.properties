# SonarCloud (SonarQube Cloud) analysis config. CI-based scan; see docs/adr/0005-sonarcloud.md.
sonar.projectKey=Bernice-Koh_pattern-mirror
sonar.organization=bernice-koh

# Monorepo as one project: both source trees, backend tests called out so Sonar
# scores them as tests, not as production code to be covered.
sonar.sources=backend/src,frontend/src
sonar.tests=backend/tests
sonar.python.version=3.12

# Coverage from each stack's CI test run (pytest-cov, vitest --coverage).
sonar.python.coverage.reportPaths=backend/coverage.xml
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info

# Generated / vendored / migration code, and co-located frontend test files, are
# not production source to lint or cover.
sonar.exclusions=**/migrations/**,frontend/dist/**,frontend/coverage/**,**/node_modules/**,frontend/src/**/*.test.ts,frontend/src/**/*.test.tsx
sonar.coverage.exclusions=backend/tests/**,**/migrations/**,frontend/src/main.tsx,frontend/src/router.tsx,frontend/src/test/**

# Fail the CI step when the gate fails, so a red gate shows as a failing check.
sonar.qualitygate.wait=true
